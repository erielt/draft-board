{
  "manifest_version": 2,
  "name": "Draft Board Redirect",
  "description": "Redirect the draft application to the Draft Board App.",
  "version": "0.1",
  "permissions": [
    "webRequest",
    "webRequestBlocking",
    // TODO(erielt): Only request permissions for the required URLs.
    "<all_urls>"
  ],
  "background": {"scripts": ["background.js"]}
}
